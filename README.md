# Hairoscope UI

To run the application, run and debug (or press F5) while connected to the Windows emulator. The software requires Android Studio to view in mobile devices.

app.py must be running in order for the product recommendation page to display its respective products.
